This is a list of files that needs their strings changed into i18n functions(?). 
This file may be refactored to list files that we need to be translated into a language in the future.

```
/app/controllers/* (Some of them are done but half assed)

```