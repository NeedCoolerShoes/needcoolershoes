<!DOCTYPE html>
<html>
  <head>
    <title>
      <%= content_for?(:title) ? "#{yield(:title)} -" : nil %>
      Miners Need Cooler Shoes :: A fan made Restoration of Need Cool Shoes
    </title>

    <%= render partial: "shared/meta" %>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= content_for?(:scripts) ? yield(:scripts) : nil %>
    <%= render partial: "shared/favicons" %>
    <%= render partial: "shared/plausible" %>
  </head>

  <body class="flex flex-col min-h-screen">
    <%= render partial: "shared/flash" %>
    <div class="sm:mx-16">
      <%= render partial: "shared/header" %>
    </div>
    <main class="flex flex-col items-center justify-start grow mx-3 sm:mx-16">
      <div class="flex flex-col max-w-ncs-content w-full">
        <% if content_for?(:message) %>
          <%= yield :message %>
        <% else %>
          <div class="h-3.5">
          </div>
        <% end %>
        
        <%= yield %>
      </div>
    </main>
    <%= render partial: "shared/footer" %>
  </body>
</html>
