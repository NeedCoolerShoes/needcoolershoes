<div class="w-full bg-ncs-tweed bg-ncs-gray-700 rounded text-white overflow-clip">
  <div class="flex justify-center w-full p-4">
    <div class="w-full max-w-xl prose-lg">
      <h3>Set-up Multi-Factor Authentication</h3>
      <p>
        Now you will need to add your one-time password secret to a
        authentication app, or a password manager. Apps like Authy, or
        password managers like 1Password will work.
      </p>
      <div class="flex items-center justify-center p-4 rounded bg-white">
        <%= @otp_barcode.html_safe %>
      </div>
      <div class="pt-4">
        <p>
          Scan the above QR code with your authentication app of choice.
        </p>
        <p>
          Or, enter the following code: <code class="p-1 bg-ncs-gray-600"><%= @otp_secret %></code>
        </p>
        <p>
          Once it is set up, enter the generated code below, to continue.
        </p>
      </div>
      <div class="flex flex-col-reverse xs:flex-row items-center justify-between gap-2 pt-4">
        <%= link_to "Cancel", edit_user_registration_path, class: "btn-ncs no-underline" %>
        <%= form_with scope: :otp, method: :put, url: otp_path do |form| %>
          <div class="flex gap-2 items-start">
            <%= form.text_field :otp_attempt, placeholder: "123456", class: "h-10 text-black" %>
            <%= form.submit "Verify", class: "btn-ncs px-2" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>