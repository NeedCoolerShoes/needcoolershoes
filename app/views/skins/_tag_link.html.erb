<!-- NOTHING TO TRANSLATE -->
<%= 
  link_to tag.name,
  skins_gallery_path(params.merge(tag: tag.name)),
  title: "#{tag.name} - #{tag.taggings_count}",
  onclick: "event.stopImmediatePropagation()"
%> 