<!-- NOTHING TO TRANSLATE -->

<%= render partial: "shared/welcome" if params[:welcome].present? %>

<% content_for :styles do %>
  <%= stylesheet_link_tag "skins" %>
<% end %>

<% content_for :filters do %>
<% end %>

<% content_for :filters do %>
  <% if can_use_gallery_filters? %>
    <%= render partial: "skins/index/skin_filters", locals: {params: @gallery_params, active_tags: @active_tags, tags: @tags} %>
  <% else %>
    <div class="bg-ncs-dark-700 p-2 pb-4 rounded-b text-ncs-dark-100">
      <p class="mb-2">
        Unfortunately, due to an increase in bot traffic, advanced search features have been temporarily disabled for users that are not signed in.
      </p>
      <p>Please <%= link_to "sign in", new_user_session_path, class: "underline" %> for advanced search.</p>
    </div>
  <% end %>
<% end %>

<% content_for :before_gallery do %>
  <div class="md:hidden w-full pt-2">
    <%= render partial: "shared/search", locals: { classes: "w-full", btn_classes: "btn-ncs shrink w-auto" } %>
  </div>
<% end %>

<% if @jam.present? %>
  <div class="mt-2">
    <%= render partial: "skins/index/jam_info", locals: { jam: @jam } %>
  </div>
<% end %>

<%= render partial: "shared/pagy_nav", locals: { pagy: @pagy } %>
<div class="flex flex-col mob:flex-row-reverse justify-between mb-2 -mt-2">
  <h1 class="text-ncs-dark-200 font-bold">Minecraft Skins</h1>
  <% if @skins.any? %>
    <p class="text-ncs-dark-200 text-sm">
      Showing skins
      <span class="font-bold"><%= @pagy.from %></span>
      to <span class="font-bold"><%= @pagy.to %></span>
      of <span class="font-bold"><%= @pagy.count%></span>
    </p>
  <% end %>
</div>

<div class="grow">
  <% if !@skins.any? %>
    <div class="flex flex-col gap-2 items-center">
      <h1 class="text-4xl font-bold text-white">NO SKINS?</h1>
      <img src="<%= asset_path "megamewnd.png" %>" width="350" height="465.45" alt="Mascot">
      <p class="text-ncs-dark-200">Current search / filters yield no results.</p>
    </div>
  <% end %>
  <div class="grid grid-cols-1 mob:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
    <%= render Gallery::SkinCardComponent.with_collection(@skins, params: @gallery_params, show_visibility: current_user) %>
  </div>
</div>
<%= render partial: "shared/pagy_nav", locals: { pagy: @pagy } %>

<% if @pagy.page == 1 %>
<details class="text-ncs-dark-100">
  <summary class="font-bold cursor-pointer select-none">Minecraft Skin Gallery</summary>
  <div class="flex flex-col gap-2">
    <p>
      Explore our awesome collection of Minecraft Skins, made by our community. Find the perfect skin for your Minecraft character with our
      many advanced filters or combine parts in our <a class="underline" href="<%= root_path %>">editor</a> to create something cool.
    </p>
    <p>
      You can search for skins, apply tags, and filter skins by part or category. The gallery also lets you search for Slim (Alex) or Classic (Steve) model skins.
      Once you have found a skin you like, you can download it to your computer, or sign in with your Minecraft Java account to quickly
      set your skin from our site.
    </p>
    <p>
      NeedCoolerShoes is the best way to browse for high quality Minecraft Skins. The gallery is constantly moderated by our staff to ensure that
      there are no duplicates and that the skins that are available have had effort put in to them.
    </p>
  </div>
</details>
<% end %>