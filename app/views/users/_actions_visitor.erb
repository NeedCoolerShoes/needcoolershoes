<% if user.skins.any? %>
  <%= link_to "View User's Skins", gallery_path(user: user.name), class: "btn-ncs" %>
<% end %>
<% if user.favourites.any? %>
  <%= link_to "View User's Favourites", gallery_path(favourited_by: user.name), class: "btn-ncs" %>
<% end %>