<% if current_user.present? && current_user.id == @user.id %>
  <%= render partial: "show_owner", locals: { user: @user } %>
<% else %>
  <%= render partial: "show_visitor", locals: { user: @user } %>
<% end %>